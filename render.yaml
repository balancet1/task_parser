services:
  - type: web
    name: pdf-task-parser
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: HF_TOKEN
        sync: false
    healthCheckPath: /
    autoDeploy: true